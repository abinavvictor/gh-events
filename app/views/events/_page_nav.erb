<nav>
  <ul>
    <% (1..last_page).each do |p| %>
      <% if p == current_page %>
        <li class="current"><%= p %></li>
      <% else %>
        <li><%= link_to(p.to_s, "/#{login}?page=#{p}") %></li>
      <% end %>
    <% end %>
  </ul>
</nav>