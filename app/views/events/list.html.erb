<h1>
  <a href="<%= @user.url %>"><img src="<%= @user.avatar_url %>" alt="@<%= @user.login %>"/></a>
  <a href="<%= @user.url %>"><%= @user.login %></a>
</h1>

<table>
  <thead>
  <tr>
    <th>Date</th>
    <th>ID</th>
    <th>Type</th>
    <th>Actor</th>
    <th>Repository</th>
  </tr>
  </thead>
  <tbody>
  <%= render(partial: 'event_row', collection: @events, as: :event) %>
  </tbody>
  <% # TODO: pagination %>
</table>